import 'package:flutter/material.dart';

class AppColors {
  // Background & Surfaces
  static const Color background = Color(0xFF040A10);
  static const Color surface = Color(0xFF08121C);
  static const Color card = Color(0xFF0C1926);

  // Borders & Text
  static const Color border = Color(0xFF1E354D);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF8A9BAE);
  static const Color textMuted = Color(0xFF5F748D);

  // Status Colors (as requested)
  static const Color statusSafe = Color(0xFF00E676); // Green
  static const Color statusWarning = Color(0xFFFFB300); // Amber/Orange
  static const Color statusHigh = Color(0xFFFF6D00); // Orange/Red
  static const Color statusCritical = Color(0xFFFF1744); // Red
  static const Color statusInfo = Color(0xFF00B0FF); // Cyan/Blue
  static const Color statusAnalytics = Color(0xFF651FFF); // Purple/Blue
  
  // Accents / Glows
  static const Color primary = Color(0xFF00B0FF); // Cyan/Blue glow base
  static const Color blue = Color(0xFF2979FF); 
}